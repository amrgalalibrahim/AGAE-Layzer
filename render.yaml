services:
  - type: web
    name: agea-layzer
    env: python
    buildCommand: pip install -r requirements_deploy.txt
    startCommand: gunicorn app_production:app --bind 0.0.0.0:$PORT --workers 4 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

